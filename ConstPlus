const ESTANTE = {
    cor : "preto",
    nrPrateleiras : 3,
    prateleiras: [{
        numero : 1,
        conteudo : ["livro", "microfone", "estojo"]},
                {
        numero:2,
        conteudo: ["garrafa", "tosta", "garfo"]},
                {
                    numero: 3,
                    conteudo: []}

    ]
}


Cria uma constante  estante que é um objeto que deve incluir:

Uma propriedade cor com o valor de preto

Uma propriedade nrpratel com o valor de 

Uma propriedade  que é um array de objetos:

1:

Uma propriedade  com o valor de 

Uma propriedade  que é um array com os valores:

2:

Uma propriedade  com o valor de 

Uma propriedade  que é um array com os valores:

3:

Uma propriedade  com o valor de 
Uma propriedade  que é um array vazio:
